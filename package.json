{
  "name": "codex-bilingual-reader-for-zotero",
  "version": "1.4.0",
  "private": true,
  "description": "Create preserved-layout English-Chinese PDFs in Zotero with Codex CLI or OpenAI-compatible APIs.",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs && python -m unittest tests/test_batch_broker.py"
  },
  "engines": {
    "node": ">=20"
  }
}
