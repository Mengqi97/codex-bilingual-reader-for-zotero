# Security policy

Please do not file public issues containing API keys, Codex tokens, PDFs with private content, or generated cache files.

This plugin intentionally uses the local Codex CLI session and never handles `auth.json`. Security reports should include the Zotero version, plugin version, and minimal reproduction steps with all sensitive text removed.
